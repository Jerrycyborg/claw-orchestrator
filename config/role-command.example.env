# Native API path (primary):
# export OPENCLAW_API_URL='http://127.0.0.1:11449'
# export OPENCLAW_API_TOKEN='<token-if-required>'
# export OPENCLAW_API_ROLE_PATH='/api/roles/execute'

# Legacy shell bridge (fallback only):
# Template placeholders available:
# {role} {prompt} {intent} {runId} {rolePrompt} {rolePromptB64}
# export OPENCLAW_ROLE_CMD='openclaw sessions send --label pool-{role} --message "{rolePrompt}"'
