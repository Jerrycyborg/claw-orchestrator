# Copy to your shell/session before running --mode openclaw
# Template placeholders available:
# {role} {prompt} {intent} {runId} {rolePrompt} {rolePromptB64}

# Example using direct message text payload:
# export OPENCLAW_ROLE_CMD='openclaw sessions send --label pool-{role} --message "{rolePrompt}"'

# Safer example using base64 payload (decode in receiver side if needed):
# export OPENCLAW_ROLE_CMD='openclaw sessions send --label pool-{role} --message "ROLE_PROMPT_B64:{rolePromptB64}"'
